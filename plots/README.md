# Plots

Generated by `python -m sim.plots` from real run data (`runs/*/scorecard.json`) and stamped scenario bands (`scenarios/*.json`). Dependency-free SVG — re-run to refresh.

- `benchmark_scores.svg`
- `scenario_bands.svg`
